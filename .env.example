# Prisma 與 Next.js 皆會讀取此檔（Prisma CLI 預設不讀 .env.local）
# Zeabur 變數清單（說明見 lib/env-check.ts 的 ZEABUR_ENV_VARIABLES）
# Zeabur PostgreSQL：服務頁 → Connection String → 貼到下方（勿加引號）
DATABASE_URL=

# 僅開發：暫時跳過根 layout 環境檢查（勿用於正式站）
# SKIP_ENV_CHECK=1

# 檢查格式（不會顯示密碼）：npm run check:db-url

# ── Zeabur PostgreSQL：本機 dev / 本機 build 連雲端 DB ───────────────────
# 必須在 Zeabur 控制台為該 PostgreSQL 服務「開啟公網存取／Public Networking」，
# 並使用對外連線字串（非僅叢集內網 URL）；否則本機無法連線，migrate / seed / build 會失敗。

# ── NextAuth / Auth.js（若專案已整合登入）──────────────────────────────
# 部署與本機皆須設定；請使用隨機字串，勿用可猜測的值。
# 產生方式（擇一）：openssl rand -base64 32
# Auth.js v5：AUTH_SECRET=
# NextAuth v4：NEXTAUTH_SECRET=  與  NEXTAUTH_URL=https://你的子網域.zeabur.app
# AUTH_SECRET=
# NEXTAUTH_SECRET=
# NEXTAUTH_URL=

# 選用：正式網域（metadataBase、Open Graph）
# NEXT_PUBLIC_SITE_URL=https://你的子網域.zeabur.app

# ── 同步 schema 到 Zeabur PostgreSQL（本機終端機，DATABASE_URL 指向 Zeabur）──
# 1) npm run check:db-url
# 2) npx prisma migrate deploy
# 3) npm run db:seed
#
# ── Zeabur 部署 Web（CI 建置）──────────────────────────────────────────
# Web 服務 Variables 設定 DATABASE_URL；建置流程需執行 postinstall 的 prisma generate
# 遷移建議：本機或 CI 對同一資料庫執行 migrate deploy，再上線 Web
# 若 Web 建置階段需連 DB，同上須使用可從 Zeabur build 環境連到的 DB 位址與變數。
